pytest==8.3.4
# Property-based tests in tests/kernel, tests/world and tests/friday import
# hypothesis. Without it pytest fails at COLLECTION (not just at those tests),
# taking the whole suite down with ModuleNotFoundError.
hypothesis==6.155.7
flask==3.0.3
requests==2.32.5
python-dotenv==1.0.1
websockets==12.0
playwright==1.55.0
fastapi==0.115.2
uvicorn==0.30.6
pydantic==2.12.4
httpx==0.25.2
python-telegram-bot==20.6

# Captcha solving — NopeCHA is free for personal use and works with Playwright.
# Used as a fallback when a site throws a captcha at the WebAgent.
nopecha>=1.0.0
